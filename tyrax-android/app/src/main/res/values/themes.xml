<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Edge-to-edge dark theme. The status bar (clock, battery, signal) stays
        VISIBLE — we only make it transparent and draw our black background behind
        it. Do NOT use *.Fullscreen here: it hides the system status bar entirely.
        Status bar transparency and icon tint handled in MainActivity via enableEdgeToEdge().
    -->
    <style name="Theme.TYRAX" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>
</resources>
